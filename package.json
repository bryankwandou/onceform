{
  "name": "onceform",
  "version": "0.1.0",
  "private": true,
  "description": "Consent receipts and attestations for research panels, on Solana.",
  "scripts": {
    "test": "node --test extension/test/fill.test.mjs",
    "smoke": "node chain/smoke.mjs"
  },
  "devDependencies": {
    "jsdom": "^27.0.0"
  }
}
